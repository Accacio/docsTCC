\documentclass{article}
\usepackage{dot2texi}
\usepackage{tikz}

\usepackage{pnets}
\usepackage[hidelinks]{hyperref}
\usetikzlibrary{arrows,shapes,petri,external}

% \tikzexternalize[prefix=fig/petriNets/]
\tikzexternalize

\begin{document}

\newcommand{\netTitle}{dummy}

\tikzset{
  place/.style={
    circle,
    thick,
    draw=black!100, % draw=blue!75,
    % fill=blue!20,
    minimum size=6mm
  },
  extPlace/.style={
    circle,
    dotted,
    draw=black!100, % draw=blue!75,
    % fill=blue!20,
    minimum size=6mm
  },
  transition/.style={
    rectangle,
    thick,
    fill=black,
    minimum width=8mm,
    inner ysep=0.7pt
  },
  timedtransition/.style={
    rectangle,
    thick,
    fill=black,
    minimum width=8mm,
    inner ysep=2pt
  },
  inhibitor/.style={-o},
  text/.style={}
}

\include{completo_fig}

\end{document}